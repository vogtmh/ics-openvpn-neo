<?xml version="1.0" encoding="utf-8"?><!--
  ~ Content of a SelectionField: a title + current value on the left and an arrow
  ~ selector button on the right. Only the button is clickable; the label has no
  ~ action. The button opens a single-choice overlay dialog (like a ListPreference).
  -->
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <LinearLayout
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:orientation="vertical">

        <TextView
            android:id="@+id/selection_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?android:attr/textAppearanceMedium"
            tools:text="Type" />

        <TextView
            android:id="@+id/selection_value"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textAppearance="?android:attr/textAppearanceSmall"
            android:textColor="?android:attr/textColorSecondary"
            tools:text="Certificates" />

    </LinearLayout>

    <androidx.appcompat.widget.AppCompatButton
        android:id="@+id/selection_button"
        style="@style/Widget.OpenVPNNeo.SelectorButton"
        android:text="›"
        android:textSize="18sp" />

</merge>
