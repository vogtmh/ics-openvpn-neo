<?xml version="1.0" encoding="utf-8"?><!--
  ~ Copyright (c) 2012-2016 Arne Schwabe
  ~ Distributed under the GNU GPL v2 with additional terms. For full terms see the file doc/LICENSE.txt
  -->
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

    <LinearLayout

            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:orientation="vertical"
            android:paddingLeft="@dimen/stdpadding"
            android:paddingRight="@dimen/stdpadding">

        <TextView
                android:id="@+id/notification_permission"
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:padding="@dimen/stdpadding"
                android:drawableStart="@drawable/notifications"
                android:drawablePadding="@dimen/stdpadding"
                android:textColor="@color/text_primary"
                android:drawableTint="@color/text_primary"
                tools:visibility="visible"
                android:visibility="gone"
                android:text="@string/missing_notification_permission" />

        <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/vpn_list"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:clipToPadding="false"
                android:paddingBottom="88dp"
                android:descendantFocusability="afterDescendants" />

        <LinearLayout
                android:id="@+id/empty_view"
                android:layout_width="match_parent"
                android:layout_height="0dp"
                android:layout_weight="1"
                android:gravity="center"
                android:orientation="vertical"
                android:visibility="gone">

            <TextView
                    android:layout_width="fill_parent"
                    android:layout_height="wrap_content"
                    android:text="@string/no_vpn_profiles_defined"
                    android:textColor="@color/text_primary"
                    android:textAppearance="?android:attr/textAppearanceLarge" />

            <Space
                    android:layout_width="match_parent"
                    android:layout_height="12sp" />


            <TextView
                    android:layout_width="fill_parent"
                    android:layout_height="wrap_content"
                    android:autoLink="web"
                    android:textColor="@color/text_primary"
                    android:text="@string/openvpn_is_no_free_vpn" />

            <Space
                    android:layout_width="match_parent"
                    android:layout_height="12sp" />


            <TextView
                    android:id="@+id/add_new_vpn_hint"
                    android:layout_width="fill_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/text_primary"
                    tools:ignore="SelectableText" />

            <Space
                    android:layout_width="match_parent"
                    android:layout_height="12sp" />

            <TextView
                    android:layout_width="fill_parent"
                    android:layout_height="wrap_content"
                    android:textColor="@color/text_primary"
                    android:text="@string/faq_hint" />
        </LinearLayout>

    </LinearLayout>

    <!-- Floating Add Profile Button -->
    <ImageButton
        android:id="@+id/fab_add_profile"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="bottom|end"
        android:layout_margin="16dp"
        android:elevation="8dp"
        android:src="@drawable/plus_simple"
        android:background="@drawable/fab_style"
        android:contentDescription="@string/menu_add_profile" />

    <!-- Floating Main Menu Button -->
    <ImageButton
        android:id="@+id/fab_main_menu"
        android:layout_width="56dp"
        android:layout_height="56dp"
        android:layout_gravity="bottom|start"
        android:layout_margin="16dp"
        android:elevation="8dp"
        android:src="@drawable/ic_menu_hamburger"
        android:background="@drawable/fab_style"
        android:contentDescription="@string/menu_title" />

    <!-- Pill-shaped Favorites / All switcher (shown only when favorites exist).
         Match the FABs' 56dp height + 16dp bottom margin and centre the buttons
         vertically so the pill lines up with the two floating buttons. -->
    <com.google.android.material.button.MaterialButtonToggleGroup
        android:id="@+id/favorites_switcher"
        android:layout_width="wrap_content"
        android:layout_height="56dp"
        android:layout_gravity="bottom|center_horizontal"
        android:layout_marginBottom="16dp"
        android:gravity="center_vertical"
        android:elevation="8dp"
        android:visibility="gone"
        app:singleSelection="true"
        app:selectionRequired="true"
        tools:visibility="visible">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_tab_favorites"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/tab_favorites" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btn_tab_all"
            style="@style/Widget.Material3.Button.OutlinedButton"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/tab_all_profiles" />

    </com.google.android.material.button.MaterialButtonToggleGroup>

    <!-- <include layout="@layout/profile_list_fabs" /> -->

</FrameLayout>
