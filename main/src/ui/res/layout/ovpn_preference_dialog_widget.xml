<?xml version="1.0" encoding="utf-8"?><!--
  ~ Widget shown on the right of dialog-opening preference rows (ListPreference,
  ~ EditTextPreference, DialogPreference, file pickers) in the profile editor.
  ~ Sized to match the MaterialSwitch toggles. The row itself is made non-clickable
  ~ by the custom preference classes; this button is the only click target and it
  ~ triggers the preference's dialog (see SelectorPreferenceBinder).
  -->
<androidx.appcompat.widget.AppCompatButton xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/ovpn_selector_button"
    style="@style/Widget.OpenVPNNeo.SelectorButton"
    android:text="›"
    android:textSize="18sp"
    android:contentDescription="@null" />
